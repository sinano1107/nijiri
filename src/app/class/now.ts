export class Now {
  id: string;
  uid: string;
  campusId: number;

  constructor(id: string, uid: string, campusId: number) {
    this.id = id;
    this.uid = uid;
    this.campusId = campusId;
  }
}
